# tmux-osc-filter

One-line install on Linux x86_64:

```bash
curl -L https://github.com/andjohnsonj5/tmux-osc-filter/releases/latest/download/tmux -o /usr/local/bin/tmux && chmod 755 /usr/local/bin/tmux
```

Verify:

```bash
tmux -V
```
